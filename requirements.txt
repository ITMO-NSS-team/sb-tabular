# Core (data pipeline, bridge primitives, neural solvers)
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.5
torch>=2.6
tqdm>=4.66

# Boosted solvers (CatBoost field models)
catboost>=1.2

# Feature-wise (structural) solvers — DAG learning
pgmpy>=1.0
networkx>=3.2

# Hyperparameter tuning (sbtab/experiments/tuning_script)
optuna>=4.2

# Baselines
sdv          # CTGAN wrapper (CTGANSynthesizer)
tabpfgen     # TabPFGen wrapper

# Dataset acquisition
ucimlrepo
kagglehub>=1.0

# Optional
# geotorch   # LightSB with full covariance (is_diagonal=False) only
# matplotlib>=3.10  # visualization notebooks
